/**
 * Escribe una función que reciba dos arrays y devuelva un nuevo array que sea la concatenación de los dos arrays.
 * @param {any[]} arr1 - Primer array.
 * @param {any[]} arr2 - Segundo array.
 * @returns {any[]} - Nuevo array que es la concatenación de los dos arrays.
 */
export const concatenateArrays = (arr1, arr2) => {
  // Implementa tu solución aquí

  return [...arr1, ...arr2];
};
