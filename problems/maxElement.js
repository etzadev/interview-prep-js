/**
 * Escribe una función que reciba un array de números y devuelva el elemento más grande.
 * @param {number[]} arr - Array de números.
 * @returns {number} - El elemento más grande del array.
 */
export const maxElement = (arr) => {
  // Implementa tu solución aquí

  return Math.max(...arr);
};
